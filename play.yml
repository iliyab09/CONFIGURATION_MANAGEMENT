---
 - name: update webserver1
   hosts: webserver1
   
   tasks:

    - name: init db1
      become: yes
      command: "npm run initdb"
      args:
        chdir: /home/iliyab09/bootcamp-app
        
 - name: update webserver2
   hosts: webserver2
   
   tasks:

    - name: init db2
      become: yes
      command: "npm run initdb"
      args:
        chdir: /home/iliyab09/bootcamp-app